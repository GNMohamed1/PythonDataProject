# ==========================================
# EGYPT RELOCATION ADVISOR - ENV TEMPLATE
# Copy this file to '.env' and fill in real values.
# DO NOT commit the actual '.env' file to Git!
# ==========================================

# Mapbox Access Token (Optional: Required if using Mapbox base maps in Pydeck/Plotly)
MAPBOX_TOKEN=Token

# Streamlit Configuration
STREAMLIT_SERVER_PORT=Port
STREAMLIT_SERVER_HEADLESS=true

# Environment State (development | production)
APP_ENV=development

# Optional External APIs
OPEN_METEO_API_URL=https://air-quality-api.open-meteo.com/v1/air-quality
